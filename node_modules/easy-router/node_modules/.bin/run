#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-run/bin/start" "$@"
  ret=$?
else 
  node  "$basedir/../node-run/bin/start" "$@"
  ret=$?
fi
exit $ret
